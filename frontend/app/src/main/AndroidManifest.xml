<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnflow">

    <!-- Permissions (if needed) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnFlow">

        <!-- Main Activity (Entry Point) -->
        <activity android:name=".LearnActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->

        <activity android:name=".CourseActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".QuizElementActivity" />
        <activity android:name=".TextElementActivity" />
        <activity android:name=".VideoElementActivity" />

    </application>

</manifest>